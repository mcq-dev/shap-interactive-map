
const config = {
    mapbox: {
        accessToken: 'example',
        languageStyle: {
            'ar': 'mapbox://example',
            'de': 'mapbox://example',
            'en': 'mapbox://example'
        }
    },
    wp: {
        // url of the wordpress instance where the plugin is served from
        url: 'https://example.example'
    }
}

export {
    config
}